# 大白 Linux / macOS 依赖清单
#
# 内容已收敛到 requirements-core.txt（跨平台唯一权威）—— 这份文件留着是为了不破坏
# 已有脚本与文档里的引用，装出来的东西完全一样。为什么不用 requirements.txt，
# 见 requirements-core.txt 顶部说明。
#
# Linux 特有提醒：
#   - Blender 相关（bpy / bmesh / mathutils）请装系统 Blender，用 DABAI_BLENDER 指过去
#   - netifaces 需要 python3-dev / gcc；编译失败可跳过，它只用于本机 IP 展示
#
# 安装：pip install -r requirements-linux.txt

-r requirements-core.txt

# ---- 可选（装不上不影响核心对话/3D/技能）----
# pyautogui
# sounddevice
